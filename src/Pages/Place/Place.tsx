import React, { Fragment } from "react";

const Place = () => {
  return (
    <Fragment>
      <button>emporter</button>
      <button>Sur place</button>
    </Fragment>
  );
};

export default Place;
